Known deficiencies:
1) Main program and FileReader only works with shapes.
2) FileReader is only capable of reading and instantiating shape objects as defined in the assignment document. 
3) FileReader will only work with the file format as specified in the assignment document.

Missing functionalities:
